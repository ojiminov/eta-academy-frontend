// Replaced by StudentDashboard.tsx in ERP portals
import React from 'react'
import { Navigate } from 'react-router-dom'
export default function DashboardPage() {
  return <Navigate to="/student/dashboard" replace />
}
