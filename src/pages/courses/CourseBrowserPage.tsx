// Placeholder — old e-learning page replaced by ERP portals
import React from 'react'
import { Navigate } from 'react-router-dom'
export default function CourseBrowserPage() {
  return <Navigate to="/login" replace />
}
